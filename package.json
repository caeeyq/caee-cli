{
  "name": "caee-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run build && caee",
    "publish": "npm run build && lerna publish",
    "build": "npm run clean && lerna exec -- tsc && shx echo =====build-end=====",
    "clean": "lerna exec -- shx rm -rf lib && shx echo =====clear-end=====",
    "relink": "lerna exec --scope @caee/cli-core npm unlink && lerna exec --scope @caee/cli-core npm link"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.4",
    "lerna": "^4.0.0",
    "shx": "^0.3.3",
    "typescript": "^4.4.4"
  }
}
