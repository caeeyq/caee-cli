{
  "name": "caee-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run build && caee",
    "publish": "npm run build && lerna publish",
    "clean": "lerna exec -- shx rm -rf lib && shx echo =====clear-end=====",
    "build": "npm run clean && lerna exec -- tsc && shx echo =====build-end====="
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0",
    "shx": "^0.3.3",
    "typescript": "^4.4.4"
  }
}